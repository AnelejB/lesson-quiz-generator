{
  "name": "lesson-quiz-generator-netlify",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless function and frontend for the Lesson Quiz Generator",
  "dependencies": {
    "@google/genai": "^1.24.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "scripts": {
    "build": "mkdir -p dist && esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic && cp index.html manifest.json service-worker.js dist/ && (cp *.png dist/ || true)"
  }
}
