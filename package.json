{
  "name": "lesson-quiz-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Your personal AI study buddy. Create lessons from text or files and generate quizzes to test your knowledge.",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic --define:process.env.API_KEY='\"'$API_KEY'\"' && cp index.html style.css manifest.json service-worker.js dist/"
  },
  "dependencies": {
    "@google/genai": "^1.29.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}